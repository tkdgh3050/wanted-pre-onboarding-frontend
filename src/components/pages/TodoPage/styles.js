import styled from 'styled-components';

export const TodoMainWrapper = styled.main`
  min-height: var(--default-main-height);

  display: flex;
  flex-direction: column;
  align-items: center;
  align-content: center;
`;

